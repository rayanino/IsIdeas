# Immediate Next Actions

1. Run `npm install`.
2. Start the dashboard with `npm run dev`.
3. Run `npm run runtime:doctor`.
4. Run `npm run runtime:tick`.
5. Submit one real owner idea through the intake form.
6. Use the resulting queue and frontier to decide the next dossier-hardening move.
